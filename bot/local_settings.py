"""Настройки, вшитые прямо в код, — альтернатива переменным окружения.

Репозиторий приватный, владелец сознательно выбрал этот вариант вместо
Railway Variables.

Помни две вещи:
  * всё отсюда попадает в историю git навсегда. Замена значения не стирает
    старое из прошлых коммитов — утёкший ключ нужно отзывать, а не править;
  * на горячем кошельке держи только рабочую сумму, не всю кассу.

Переменная окружения всегда важнее: если та же переменная задана в Railway,
берётся она, а не значение отсюда. Пустая строка = «не задано».
"""
from __future__ import annotations

SETTINGS: dict[str, str] = {
    # ─── Обязательное ────────────────────────────────────────────────
    "BOT_TOKEN": "",
    "ADMIN_IDS": "",              # свои Telegram id через запятую
    "WALLET_MNEMONIC": "",        # 24 слова через пробел

    # ─── Режимы выплат ───────────────────────────────────────────────
    "AUTO_PAYOUT": "false",             # доля с продажи не улетает сама
    "WITHDRAW_NEEDS_APPROVAL": "true",  # вывод воркера ждёт твоей кнопки
    "MIN_WITHDRAW_TON": "0.1",
    "MAX_PAYOUT_TON": "50",
    "MAX_DAILY_PAYOUT_TON": "300",

    # ─── База ────────────────────────────────────────────────────────
    "DB_PATH": "/data/payouts.db",

    # ─── Подарки ─────────────────────────────────────────────────────
    "GIFTS_ENABLED": "false",
    "TG_API_ID": "",
    "TG_API_HASH": "",
    "TG_SESSION": "",
    "MRKT_SESSION_B64": "",
    "TG_PROXY": "",               # socks5://user:pass@host:port
    "WORKER_SHARE_PERCENT": "80",
    "UNDERCUT_PERCENT": "3",
    "MIN_LIST_PRICE_TON": "0.5",
}
